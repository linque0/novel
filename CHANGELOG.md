## [1.0.17] - 2026-09-09

### 大纲画布 · 右键连线恢复编辑浮层（连接点并入）
- **右键连线直接打开编辑浮层**：不再弹单独的「创建连接点」菜单——浮层集成全部连线操作：标签、箭头方向、关系类型、线型、颜色、单条线宽/箭头大小、删除
- **「在此处创建连接点」并入浮层**：右键打开浮层时显示该操作行（虚线按钮，与菜单 item 同 hover 语言），点击在右键落点（投影到真实路径）建连接点，浮层保持开启便于连续操作
- 编辑浮层三个入口：**右键连线**（任意时刻）、拖线建边完成自动弹出、（左键已改为拖拽塑形不再触发）
- 验收：`tools/edgeedit-ctx-verify.mjs` 3/3——右键直接开浮层无旧菜单、浮层内建点（浮层保持）、右键入口改标签生效

## [1.0.16] - 2026-09-09

### 大纲画布 · 线间连线方向修正 + 左键拖拽塑形
- **线间连线首尾倒错修复**：连接点→连接点的连线（`fromJunction`，几何正常方向）此前被箭头逻辑误判为"几何反转"，箭头画到了引出端；现按「仅 toJunction 无 fromJunction 才反转」判定，箭头始终指向接入端
- **左键点击连线改为拖拽塑形**（取消打开编辑浮层）：按住连线拖动 → 连线弯曲（`rel.custom` 控制点跟随鼠标实时预览）；拖回贴近两端点直线（垂距 < 6px）→ 自动拉直恢复自动路由；**双击连线 → 拉直**（mousedown 按 e.detail≥2 判定，不受 preventDefault 抑制）；编辑浮层唯一入口 = 拖线建边完成后自动弹出
- 塑形几何：`customEdgeGeom`（二次贝塞尔提升三次，端点仍锁定端口/连接点，箭头衔接回缩兼容）；`olnodeRelPatchAny` 通用补丁兼容 rels 与 _out 连线
- 验收（单一功能，不做全量回归）：`tools/shape-verify.mjs` 5/5——倒错（箭头距接入端 0px）、点击无浮层、拖拽弯曲、拖回自动拉直、双击拉直

## [1.0.15] - 2026-09-09

### 大纲画布 · 线连到线（连接点互连）
- **拖线落到连线本体自动建点接线**：按住连接点拖出连线，落到**另一条连线**上（12px 容差命中）时，在落点（真实路径投影）自动创建新连接点并完成连接点→连接点接线——实现从一条连线连接到另一条连线；同一连线可创建**多个**连接点、各自引出多条连线
- 落点优先级：既有连接点（直接接）> 连线本体（自动建点接）> 模块（常规建边）；拖回自身宿主连线不生效（防止自环）
- 新增 `olnodeRelAddJunctionToJunction`（连接点→连接点，同源同目标去重）；`edgeGeomFromJunction` 支持终点为连接点（两端都钉在各自宿主连线路径上，随节点挪位保持在线）；级联删除覆盖（删任一端连接点即删该连线）
- 验收：线连到线 4/4（自动建点/接线/多连接点多引出/页面健康）+ 全量回归 10/10 + 卡死 4/4 + 视觉 3/3

## [1.0.14] - 2026-09-08

### 大纲画布 · 画布卡死修复 + 箭头衔接 + 编辑浮层背景
- **修复画布打开即卡死（严重）**：连接点几何计算（`hostJunctionGeom` / `edgeGeomFromJunction`）在 `edges` computed 内部经 `edgeByOwnerRel` 回读 `edges.value`——computed 自引用触发无限重算，主线程死循环，含连接点数据的书打开画布后整体无响应；改为直接以 `edgeGeomFor` 重算宿主几何，消除所有自引用（卡死复现验证 4/4：旧数据打开 2ms 响应、挪节点重算正常、撤销响应正常）
- **箭头不再被线穿过、也不留缝**：带箭头的一端连线路径回缩（`trimEdgeD`——折线按弧长回退拐点；贝塞尔 de Casteljau 细分取段，细分参数经数值弧长搜索而非线性近似，消除端部行进差造成的 ~8px 空隙）；回缩量 = 箭头三角形底边深度（size·cos(0.42)）+ 半线宽补偿，线端恰好缩进底边内约 0.8px——三档箭头（0.5×/1×/3×）实测严丝合缝；命中热区仍用完整路径不受影响
- **编辑浮层背景修复**：`.oc-eedit` / `.rg-eedit` 此前 Teleport 到 `body` 丢失 `.app-root.theme-*` 上的主题 CSS 变量（背景透明）；改挂 `.app-root` 内，面板底色/边框/投影恢复，与模块右键菜单同一视觉语言；fixed 定位与尺寸恒定不变
- 接连接点连线（几何反转）的回缩端方向同步修正（箭头在首端）
- 回归：全链路 10/10、视觉专项 3/3、卡死复现 4/4、模型单测 48/48

## [1.0.13] - 2026-09-08

### 大纲画布 · 连接点推倒重建（连线寄生模型）
- **数据层重造**：连接点不再是画布上的模块节点（`kind:'arrow'`），改为直接挂在宿主连线数据上——`rel.junctions = [{ id, t, bornAt }]`，t 为宿主连线路径参数；连线可接入连接点（`rel.toJunction`）或从连接点引出（宿主 rel `_out` 数组）。节点挪位、整理布局、缩放后连线重算路径，连接点**始终钉在所在连线上**
- **创建高亮 → 淡出**：连接点创建后 1.6s 内加深显形（实色圆点 + 光环）方便确认位置，bornAt 过期后经 0.45s 过渡淡出回微观透明态（半径 2.5、透明度 0.14，不遮挡连线；悬停显形 0.75）
- **双向接线柱**：连接点既可被模块锚点拖线接入（终点），也可**直接按住拖出连线**（起点，落到目标模块即建边）；两方向都以连接点中心为端点、沿宿主连线切线方向零桩直出
- **右键交互定稿**：右键连线仅「创建连接点」（真实路径几何投影取 t）；右键连接点仅「删除」（接入/引出的连线级联删除）；编辑浮层只由左键点击连线触发
- **热区事件修复**：`.oc-junc-hit` 显式 `pointer-events: all`（父级 `.om-edges` 为 none，此前 mousedown 引出拖线无法命中）
- **编辑浮层美术对齐**：`.oc-eedit` 与模块右键菜单同一视觉语言——「编辑连线」标题行、属性区分隔线、同款内距/圆角/投影；Teleport + fixed 保持尺寸不随画布缩放
- **兼容清理**：旧版本遗留的 `kind:'arrow'` 模块不再进入渲染与统计；空白菜单不再提供连接点
- 使用手册 5.2 节同步；`tools/junction-fix-verify.mjs` 全链路验收 10/10 通过

## [1.0.12] - 2026-09-07

### 大纲画布 · 连接点交互定稿
- **右键连线仅保留「创建连接点」**（移除菜单中「编辑连线」项）：连接点直接创建在右键落点、吸附到被右键连线的路径最近点；连线编辑浮层只由**左键点击连线**触发
- **连接点微观透明**：6px 低透明度（0.18）圆点，默认几乎隐形、不遮挡所吸附的连线；悬停/选中时显形（放大 + 光晕）便于操作
- **无缝衔接**：任一端为连接点的连线 stub=0——新连线从连接点中心（即原连线上）零桩直出，与原连线视觉无缝衔接
- 使用手册 5.2 节同步

## [1.0.11] - 2026-09-07

### 大纲画布 · 右键连线创建连接点
- **右键连线不再直接打开编辑浮层**，改为弹出连线菜单：**「创建连接点」**（在右键落点生成连接点，自动吸附到该连线路径上最近点）+ **「编辑连线」**（原编辑浮层入口保留：标签/线型/颜色/箭头/单条线宽与箭头大小）
- 连接点创建后自动选中，四向锚点立即可拖线连接
- Esc / 点击菜单外关闭；使用手册 5.2 节同步

## [1.0.10] - 2026-09-07

### 大纲画布 · 连接点（线连线）
- **连接点组件**：v1.0.9 的箭头模块重新设计为「连接点」（接线柱）——主题色小圆点，四向锚点可拖线，实现**连线与连线之间的互连**
- **拖线落线自动建点**：从任意模块锚点拖线，落点不在模块上但贴近某条既有连线（<12px 命中容差）时，自动在该处生成连接点并完成连接；建边失败自动回滚
- 连接点不作为避障障碍（线从其上穿过是常态）；高度按实际热区渲染（20px）；空白右键「连接点」可手动放置
- 移除 v1.0.9 旋转/等比缩放交互（连接点无方向性，无需旋转）
- 使用手册 5.2 节同步

## [1.0.9] - 2026-09-07

### 大纲画布 · 箭头组件与单条连线调整
- **箭头模块**：新增可自由添加的指向箭头（空白右键「箭头」）——拖动摆位；悬停顶部 ⟳ 手柄拖拽旋转（15° 吸附）；右下角手柄等比缩放；右键菜单快捷旋转 ±90° / 180° / 重置朝向 + 删除；渲染为主题色三角（`olnodeSetRot` 存储 rot 字段）
- **单条连线尺寸**：连线编辑浮层新增「线宽 / 箭头」两行滑杆——按条覆盖画布全局设置（rel.edgeWidth / rel.arrowScale），拖动即时生效，↺ 一键恢复跟随全局；⚙ 设置中的全局滑杆继续作为默认值
### 导出 · 批量判定简化
- 全选/整卷不再强制选择合并或 zip——是否弹出批量方式选择**仅由选中章节数是否超过阈值（默认 30，可调）决定**（v1.0.8 前整卷/全选一律强制）
- 使用手册导出详解与画布章节同步更新（箭头模块 / 单条连线调整 / 新判定规则）

## [1.0.8] - 2026-09-07（交付版）

### 交付
- **安装程序已更新至 `F:/deepseek workplace/小说工坊/novel-studio-setup-1.0.8.exe`**（76.1 MB，NSIS 向导式含安装路径选择）
- 打包冒烟 3/3 通过：界面渲染 / mammoth Word 解析 / Dexie 数据库打开（一次性 packtest profile，测后已删）
- 本版内容：帮助入口双页面覆盖（书架页顶栏 + 书内顶栏均可打开内置使用手册）

# 版本更新历程（CHANGELOG）

> 每个版本 = 一个特性粒度；详细背景见 [docs/工作报告书.md](docs/工作报告书.md) 与《小说创作软件开发计划》（仓库外 `../小说创作软件开发计划.md`）。
> 所有版本均配 CDP 验收脚本入库 `tools/`，发布前全量跑一轮。
> **推送纪律（2026-09-03 更新）**：单分支直推——main = 开发主体（最新可用版本），每次代码更新同步变更版本号（package.json / README / CHANGELOG / 提交信息四处一致），不新建/删除版本分支，里程碑发布点打 tag 存档。

## v1.0.8（2026-09-07）帮助入口双页面覆盖

- 书架页顶栏（统计旁）新增「帮助」按钮，与书内顶栏入口同开「帮助 · 使用手册」弹窗（HelpManual 挂载于 App 全局层，两页面共享）；
- 针对性验证：书架点帮助 → 弹窗出现；进书顶栏点帮助 → 弹窗出现，双路径 PASS；构建通过。

## v1.0.7（2026-09-07）使用手册重写 + 内置应用

- **使用手册重写**（docs/使用手册.md）：功能介绍补齐 v1.0.2–v1.0.6 全部新能力（右键操作菜单、容器跟随动画、连线/箭头大小、导出三格式与批量策略、单次确认导出）；新增**键位详解**章节（全局/正文/画布/幕布/关系图/搜索六组，键位逐一从源码盘点核对）；导出详解独立成章（四种范围 × 三种格式行为表）；FAQ 扩至 7 条；
- **内置应用**：顶栏新增「帮助」按钮 →「帮助 · 使用手册」弹窗——手册 md 以 ?raw 打包进构建 + marked 渲染（标题/表格/代码样式贴合主题令牌），与源码手册同源永不失同步；
- **工程**：vite.config 显式锚定项目根（root: import.meta.url），修复 shell cwd 句柄失效时构建找不到入口的问题；
- **验证（针对性）**：帮助弹窗实测——12 节标题 / 9 表格 / 键位·导出·右键内容齐全 / 可滚动；构建通过。

## v1.0.6（2026-09-07）逐章分批导出仅确认一次位置

- **交付**：安装程序 `novel-studio-setup-1.0.6.exe` 已更新至小说工坊文件夹（打包冒烟 3/3 通过）；本次打包因 release2 目录被 bash 句柄锁定，按「文件锁则换输出目录」惯例临时切 release3 构建，配置已还原（本地零提交差异）；
- **多章逐章导出不再每章弹保存对话框**：首个章节弹一次确认导出位置，后续章节自动依次写入同目录（同 `章节名.格式` 命名）；首步取消即中止全部；完成提示带出导出目录；
- **实现**：electron `export:save` IPC 新增 `dirPath` 参数（给定时跳过对话框直接写该目录，自动建目录）；saveTextFile 透传 dirPath；exportChapterRowsSequential 首章取回保存路径提取目录、后续章节携带目录直写；
- **验证（按 2026-09-07 新规则做针对性验证）**：dirPath 直写链路实测——两次带目录 IPC 均无对话框落盘成功、目录自动创建、内容完整；顺序逻辑（首章对话框→取目录→后续直写）代码走查确认；构建通过。

## v1.0.5（2026-09-07）导出策略：单章命名 / 逐章分批 / 大批量双模式（合并·zip）

- **单章导出以章节名命名**：选中 1 章时文件名 = `章节名.格式`（不再是书名）；
- **多章（≤阈值且非整卷/全选）逐章分批导出**：按勾选章节的卷序+章序依次弹保存对话框，每章一个独立文件（章节名命名）；任一步取消即中止后续，已导出保留；
- **大批量（整卷 / 全选 / 超过阈值，默认 30 章）弹导出方式选择**：①合并为单个文件（按顺序合并，书名命名，txt/md/docx）；②按章打包 zip——每章一个所选格式文件，`序号-章节名` 命名并按卷分目录，压缩为一个 zip（`书名-N章.zip`）；
- **阈值可调**：批量选择区内 NInputNumber 直接改（2–500），存 appconfig 全局键 `exportBatchThreshold` 即改即存；
- exporter 新增 exportSingleChapter / exportChapterRowsSequential / exportRowsAsZip（zip 内卷目录 + 序号前缀），safeName 统一文件名安全化（非法字符替换、限长 60）；
- **验证**：DOM 六场景（全选→批量区出现/单章→章节名按钮/2 章→逐章/全选复现/zip 模式按钮切换/阈值控件）全通过；回归 olcanvas 29/29、chargraph 12/12、canvas-model 48/48、relfollow 15/15。

## v1.0.4（2026-09-07）正文导出重构：按章节勾选 + 新增 Word (docx) 格式

- **导出对话框（ExportDialog）**：导出菜单原「导出本书 TXT / Markdown」两项合并为「导出正文（选章节 · TXT/MD/Word）…」——按卷分组勾选章节（全选/按卷选/单选，默认全选=原整书行为），格式三选：TXT 纯文本 / Markdown / **Word (docx)**；
- **docx 生成（零新依赖）**：JSZip 手组 OOXML（WordprocessingML）——保留富文本基础语义（加粗/斜体/下划线/删除线/字体色、h1-h6 标题样式、对齐、列表与引用缩进），书名/卷/章节标题映射 Heading 1/2/3，A4 版式，元数据写入书名作者；
- **修复：fmt 缺失的历史章节内容**——纯文本分支遇含 HTML 标签内容自动按 html 识别（此前原样转义出 `&lt;p&gt;` 标签残留），txt/md/docx 三格式统一受益；
- buildBookText 抽出 collectExportChapters（卷序+散章兜底），支持 onlyIds 过滤；saveTextFile 支持 base64 二进制（docx 经 Electron 保存对话框落盘）；
- **验证**：GUI 全链路——对话框勾选树（卷分组/三格式）、仅选 1 章时 txt/md 只含该章、docx 生成后经 mammoth 反解析内容干净（标题+正文段落无标签残留）；回归 olcanvas 29/29、chargraph 12/12、canvas-model 48/48、relfollow 15/15。

## v1.0.3（2026-09-07）连线与箭头大小调节

- **新增「箭头大小」画布设置**（大纲画布 ⚙ 设置弹层）：0.5×–3.0× 滑杆，与既有「连线粗细」（0.8–5px）并列，canvasPrefs 新增 `arrowSize` 随作品持久化（appconfig）；
- **人物关系图线宽接入偏好**：线宽由写死 1.7 改为跟随 canvasPrefs.edgeWidth（两画布观感统一，在任一画布调粗细另一侧同步生效）；
- **验证**：GUI 断言箭头三角形顶点距 7px → 21px（3.0× 精确缩放）、stroke-width 3.4 生效；回归 olcanvas 29/29、chargraph 12/12、canvas-model 48/48、relfollow 15/15。

## v1.0.2（2026-09-07）容器模块调整大小实时跟随鼠标 + 平滑过渡

- **修复：容器拖拽调整大小时盒子完全不动、松手才跳到终值**——`containerRect` 派生盒的 base 直读库内 `effSize(n)`（n.w/n.h），绕过了拖拽用的 `dragSize` 实时尺寸，响应链断裂；base 改经传入的 `sizeOf(n)` 读取（拖拽中 = dragSize 实时值），盒子随鼠标逐帧更新；
- **新增 `resize-smooth` 平滑跟随**（美化方案 §4.4）：容器拖拽期间宽度/高度/位移四轴 0.18s `--ease` 过渡，每次 mousemove 重定向过渡形成「追着鼠标走」的跟随动画；非容器保持 0.08s 跟手短过渡；
- **验证**：GUI 拖拽中途采样——盒子高度随鼠标连续变化（707→901px）、`resize-smooth` 类挂载、松手落库精确值；回归 olcanvas 29/29、chargraph 12/12、canvas-model 48/48、relfollow 15/15。

## v1.0.1（2026-09-07）大纲画布模块操作栏改右键触发

- **交互修复：mini 工具栏不再「选中即显示」**——原点击模块后操作栏常驻遮挡模块内容且无法关闭；现改为**右键模块弹出统一操作菜单**（标题显示模块名；形状▸ / 编辑内容 / 透明度滑杆（文本框）/ 自定义类型▸ / 固定·取消固定 / 删除（红标）；容器分支调色板并入同一菜单），右键同时选中模块；
- **关闭途径**：点空白或其他区域、Esc、执行删除或进入编辑；形状/类型/固定操作后菜单保持，便于连续调整；
- 菜单容器加 `@contextmenu.stop` 防再次右键穿透画布；回归 olcanvas 29/29（两处旧断言同步新交互）、chargraph 12/12、canvas-model 48/48、relfollow 15/15；此更新不重新打包交付。

## v1.0.0（2026-09-07）首个正式版（里程碑）

- **里程碑交付**：v0.1–v0.4.19 全部能力转正——正文富文本编辑、大纲双视图（幕布树 + 自由画布）、人物关系图、设定幕布树、灵感收集、`[[双链]]` 跨模块互联、全局搜索、字数统计、回收站、章节书签、拆窗多窗口、三主题（宣纸/羊皮纸/暗夜书房）与沉浸模式、画布连线严格端口 + Hanan 稀疏网格 A* 全局最短路由、OIcon 内联 SVG 图标体系与视觉令牌体系；
- **交付形态**：Windows 安装程序（electron-builder NSIS，maximum 压缩），打包冒烟（界面渲染 / mammoth Word 解析 / Dexie 数据库打开）通过后交付；
- 本地优先：全部数据存本机 IndexedDB，输入即存；`--profile` 多配置隔离。

## v0.4.19（2026-09-07）视觉美化合并：美化测试分支合入主开发（OIcon 图标体系 + 组件质感）

- **合并**：`v0.4.17分支美化测试`（4c51139 批次一 + a35e0fd 批次二 + b41de7d stop 增强）合入主开发，零冲突；
- **OIcon 图标体系（美化方案 §6.9）**：全站 emoji 退出交互位，新增内联 SVG 图标组件（24 viewBox / 1.5px 描边 / 圆帽圆角 / 继承 currentColor，38 图标）；KIND_META / TYPE_META 图标值名称化由组件渲染；CSS 图钉改 data-URI SVG；
- **设计令牌扩充（§4）**：三主题 `--accent-deep / --text-faint / --danger-soft / --shadow-node(-hover)`，`:root` 增 `--ease / --dur-*` 动效令牌与 radius/space/fs 不变量；**组件质感（§6）**：画布节点阴影三级、浮层统一 md 圆角 + pop 投影、输入框聚焦环与占位色、导航 rail 激活态重构（accent-soft 底 + 内缘竖条 + 昼夜一键切换）；
- **基础观感（§7 批次一）**：全局细条滚动条、focus-visible 键盘焦点环、浮层 140ms 入场动效、`prefers-reduced-motion` 全局降级；
- **工具**：`npm run stop` 清理范围纳入 vite 与 electron 的 node 包装进程，新增 5173 释放校验；
- **验证**：canvas-model 48/48、chargraph 12/12、olcanvas 29/29、relfollow 15/15（dev 实例）；宣纸/暗夜双主题截图核对。

## v0.4.18（2026-09-06）测试数据隔离：npm run dev 挂 --profile=dev，验证书与真实书目分离

- **变更：开发调试版数据独立**——`npm run dev` 启动参数挂 `--profile=dev`（数据目录 `%APPDATA%\小说工坊-dev`），9 本测试验证书（关系图/画布A·B/画布右键/关系跟随/双链/多窗口/侧栏验证书 + 验收测试作品）已迁入该目录；`npm start` 正式启动版只含真实书目（浮世、星尘旅人），不再混入测试数据；
- **规则同步**：工作报告书第八节·调试与测试纪律第 3 条二次修订（验证书归属开发调试版，CDP 验收连 `npm run dev` 或 `electron . --profile=dev`；npm start 不写测试数据）；5 个验收脚本头部运行前提同步更新；
- **数据操作**：默认库验证书整库转储 → 导入 dev profile（9 本 124 行 0 跳过）→ 默认库级联删除（103 行），迁移后默认库 2 本、dev 库 9 本。

## v0.4.17（2026-09-05）修复：章节锚点卡文字偏下——状态点未定义样式撑出行盒

- **修复：锚点卡（正文卡）标题/字数行贴着卡片下半截**——`.ol-dot`（章节状态点）自 v0.4.x 引入以来**没有任何基础样式**：空的内联 `<span>` 在 `.oc-body` 前撑出一个 ~19px 的匿名行盒，把正文整体下推 23px（卡片高 48px 时文字掉到下半截、底部被裁剪）；现补齐为**绝对定位的状态徽标**（9px 圆点悬于卡片右上角，草稿=灰 / 完成=绿 / 待修改=橙，悬停仍显状态提示），完全不参与布局；实测正文偏移 23.2px → 0.8px；
- **验证**：canvas-model 48/48、olcanvas 29/29、chargraph 12/12、relfollow 15/15（回归含 v0.4.16 路由重写全量用例）。

## v0.4.16（2026-09-05）避障路由再重写：Hanan 稀疏网格 A* 全局最短路，消除重复路线与绕路

- **修复：绕行产生无意义重复路线与巨型绕路**——v0.4.15 的「固定首形 + 贪心逐段绕行」在端口背对/多障碍时按全部障碍包络外环绕，叠加桩长延伸（矩形宽高 + margin）与逐段外扩，绕出远超所需的巨型矩形，且绕行通道与包络通道重合时出现来回重复段（人物关系图错位两节点即复现）；
- **路由算法重写（routeOrthogonal，两画布共享）**：①仍先试最简首形（Z / L，无碰撞直接用，保持观感与性能）；②被挡或端口背对时改为在**障碍膨胀边界（±margin）与桩端点张成的 Hanan 稀疏网格上跑 A***，代价 = 曼哈顿段长 + 转向惩罚（少弯优先），全局择优——贴哪个障碍、绕哪一侧、走哪条通道全按最短决定，不再有包络大环绕；③求得最短折线后再做**直连 / L 形拉直收敛**，消除网格化小折；④绕行段与障碍至少保持可视间隙（2px 皮肤测试），首末段方向严格保持端口出/入桩方向；
- **桩长回归短桩（18px）**：取消「延伸越过整个矩形」——延伸桩正是巨型环绕的成因之一，A* 全局择优后无需借长桩避障；旧贪心算法完整保留为病态布局（节点重叠致网格不可达）兜底；
- **验证**：canvas-model-test 扩至 **48/48**（新增：错位背对端口最小环绕总长有界（旧算法 >1100px → 新 756px）/ 无重复共线段 / 远处障碍不影响直连 / 重叠节点病态布局仍合法绕行，另附 200 组随机布局模糊全部合法）；回归 chargraph 12/12、olcanvas 29/29、relfollow 15/15（连跑两轮幂等通过，含 80 点路径采样不入模块矩形）；性能：40 障碍单次路由 0.024ms，拖拽逐帧重算无压力；
- **测试修复：relfollow 跨轮残留误报**——`w.olnodes` 含软删幽灵节点，颜色编辑回读 `olnodes.find(...)` 会命中上一轮被删节点的旧关系（第二轮起必失败）；回读处补 `!n.deletedAt` 过滤（测试 10 / 10b / 13 三处），套件恢复幂等。

## v0.4.15（2026-09-05）端口补记 + 避障算法修正：两端节点纳入障碍、反向端口外环绕拓扑

- **修复：旧连线端口仍漂移**——v0.4.14 只对"新建时带端口记录"的连线生效，存量连线渲染时按当前位置每帧重算接口；现在首次渲染时**一次性补记** `fromSide` / `toSide`（两画布均接入，写回 relations / olnodes rels），此后端口永久锁定；
- **修复：连线穿过端点模块自身**——v0.4.14 障碍集排除了两端节点，端口严格锁定后（如目标挪到源正上方而接入口仍是"左口"）连线中段会从模块身体上穿过；现在**两端节点本体加入障碍集**，桩长自动延伸越过源/目标矩形，中段一律绕开；
- **路由算法重写（routeOrthogonal）**：①初始拓扑按端口朝向分段——两端口同向且目标在前方走标准 Z 形，**端口背对（目标在行进方向后方）改走障碍包络外环绕通道**（取离两端较近的外侧），消除反向折返段；②同一障碍被多段命中时逐次外扩 margin，不再贴边互穿；③循环上限提到 24 轮保收敛；
- **验证**：canvas-model-test 扩至 **40/40**（新增：两端节点本体不被连线穿过 / 起止点仍在记录端口 / 桩长越过宽源节点 / 高障碍多段绕行）；relfollow 15/15、chargraph 12/12、canvasctx 6/6、sidebar 5/5、multiwindow 11/11、olcanvas 29/29、dl 20/20、cdp-test 27/29（2 项存量）。

## v0.4.14（2026-09-05）连线端口严格化 + 途经模块自动绕行

- **连线接口严格锁定**：连线建立时记录**两端接口**（大纲 fromSide 沿用 + 新增 toSide；人物关系新增 `fromSide` / `toSide` 字段，落 relations 行），渲染**严格使用记录的接口**——从哪个接口拖出永远从哪个接口出、接入侧同样固定；节点任意挪位后连线起止点不再按相对位置重算漂移（旧数据无端口记录时按当前位置一次性推导并沿用，同样不再随挪位变化）；
- **途经模块自动绕行**：新增 `routeEdge` 路由引擎（canvas-model 共享）——连线出发前对途中全部模块矩形做穿越检测（贝塞尔 12 点采样），命中即改走**正交折线绕障**（沿端口外法线出桩，逐段绕行取近侧，拐角圆角 Q 过渡，多轮处理多障碍），折线任何线段不与模块矩形重叠；无障碍时保持原贝塞尔曲线观感；大纲侧障碍排除两端节点与所在容器（容器内子模块互连不误判），人物图同样接入；
- **验证**：canvas-model-test 扩至 **36/36**（端口起终点精确、避障改折线、逐段不穿障碍、无障碍保持贝塞尔、南北向端口、sideBetween、polyMid、拐角圆角）；relfollow-verify 扩至 **15/15**（新增：节点拖到源上方后起止点仍严格落在记录接口的画布坐标 ±1px、80 点路径采样不落入途中模块矩形）；回归 canvas-model 36/36、chargraph 12/12、canvasctx 6/6、sidebar 5/5、multiwindow 11/11、olcanvas 29/29、dl 20/20、cdp-test 27/29（2 项存量）。

## v0.4.13（2026-09-05）连线颜色渲染真因修复：遗留 CSS 覆盖 + 右键交互

- **修复：连线本体颜色/粗细不生效的真因**——`themes.css` 中遗留 v0.4.0 思维导图视图（OutlineMap，v0.4.1 已退役）的规则 `.om-edges path { stroke: var(--border); stroke-width: 1.6 }`。CSS 属性优先级高于 SVG 表现属性（stroke="#xxx"），因此大纲/人物两条画布**所有连线本体永远渲染为边框灰、粗细锁死 1.6**——改色后只有不受该规则影响的箭头（polygon）与标签（HTML div）变色，与用户反馈完全吻合（"颜色改变的是连线中段的块和箭头"）。上一轮验证只查了 DOM 属性未查 computedStyle/实际渲染，故未能发现；本轮已移除该规则（保留 fill:none 防命中路径渲染黑块），各线自身的 stroke-width 属性（大纲全局粗细设置/人物图 1.7）恢复生效；**像素级验证**：改色后对连线中点截屏采样，大纲 dist=7、人物图 dist=0（0=精确色值）；
- **修复：右键连线弹"新建"菜单**——Chromium 对 SVG `pointer-events:stroke` 路径的 contextmenu 命中与 mousedown 不一致（mousedown 命中路径、contextmenu 却派发给画布），且可见标签/编辑浮层也无右键处理，右键连线会冒泡到画布弹出"＋ 新建人物/在此处新建模块"菜单，严重误导；现两画布 onCanvasCtx/onBlankCtx 用 elementFromPoint 反查真实落点（点在连线/标签上→打开编辑浮层），浮层与标签补 contextmenu.prevent.stop，命中路径加 data-rel 属性；右键空白弹新建菜单的原功能保留；
- **体验**：连线命中带 16→20px，悬停可见线加粗（hover 相邻兄弟选择器）反馈可点击，命中线加 title 提示（"点击或右键编辑连线"）；
- **验证**：relfollow-verify 13/13（含右键路径专项）；回归 chargraph 12/12、canvasctx 6/6、sidebar 5/5、multiwindow 11/11、canvas-model 25/25、dl 20/20。用户真实数据全程只读+可回滚操作（测试改色已还原）。

## v0.4.12（2026-09-04）连线颜色/线型修复：大纲补齐色板 + 两画布浮层可视性

- **修复：大纲画布连线颜色无法修改**——大纲边编辑浮层此前只有关系类型（关联/因果/伏笔/冲突）、箭头与线型行，**没有颜色色板**（线色写死为按类型派生的 relColor(kind)），用户无从改色；现补齐 **主题色 + 六色调色板** 行（与人物关系图同款 EDGE_COLORS，抽入 canvas-model 共享），自定义色即时跟随**连线 stroke、箭头填充、标签边框/文字**三点；点「主题色」恢复按类型自动配色；自定义 color 存于节点 rels 项 `color` 字段（随 olnodes 落库，旧数据缺省自动配色，无需迁移）；
- **修复：边编辑浮层被画布裁掉**——两个画布（大纲 oc-eedit / 人物 rg-eedit）的编辑浮层固定向上弹出（translate(-50%, -100%-14px)），连线中点靠近画布可视区顶部时整个浮层被 `.om-canvas` 的 overflow:hidden 裁出可视区，用户点线后"什么都看不到"，体感即"无法修改颜色线型"；现两画布统一 **popPlacement**：中点上方空间不足时**翻转向下弹出**（.flip 类），并横向夹取在可视区内（±120px 半宽余量），浮层任何位置都完整可见；
- **验证**：relfollow-verify 扩至 **13/13**（新增：大纲色板出现+自定义色跟随线/箭头/标签、主题色恢复自动配色、真实拖拽平移使连线贴近可视区顶部时浮层翻转向下且完整可见）；本次验证全程采用 CDP Input 域**真实鼠标事件**（含命中测试），修正了过去 dispatchEvent 绕过命中测试的验证盲区；回归 canvas-model 25/25、chargraph 12/12、sidebar 5/5、canvasctx 6/6、multiwindow 11/11、dl 20/20；olcanvas/cdp-test 本轮在锁屏（窗口被遮挡渲染挂起）环境下受阻于重载/过渡类检查项，受阻项均与本次改动无关且上一版（v0.4.11）全量通过，环境恢复后建议复跑一轮。

## v0.4.11（2026-09-04）关系图体验增强批次

- **拖线热区拓宽 + 四边触发**：人物块边缘锚点从 2 个（左右）扩为 **4 个（上下左右）**，热区从 9px 方点改为 **16×16 半透明圆点**（悬停显形放大），连线触发区域明显更容易点中；
- **关系线型与颜色**：关系边支持**实线 / 虚线 / 点线**三线型与**六色调色板 + 主题色**（沿用大纲 EDGE_STYLES 与容器调色板），在边编辑浮层内设置，渲染即时跟随（stroke + stroke-dasharray）；数据落在 relations 表新列 `color` / `style`（Dexie 无 schema 约束，旧数据缺省为实线主题色，无需迁移）；
- **角色定位重构**：下拉枚举改为**自定义输入框**（任意角色定位自由填写）+ 8 个常见故事定位快捷胶囊（主角 / 配角 / 反派 / 导师 / 红颜 / 盟友 / 旁观者 / 龙套），点选填入、再点取消；侧栏徽标与画布节点同步显示自定义定位（未知定位用中性色兜底）；
- **修复：字段名无法编辑**——字段名原用 NInput 绑定 `@change`（值回调签名不匹配导致改名静默失效），改原生 input change 提交并保留值迁移；空名 / 重名自动忽略；
- **预置默认字段**：新建人物（无任何字段时）自动补 **外貌 / 性格** 两个空字段（可删可改名），配合 v0.4.10 空内容新建形成"建卡即有骨架"；
- **验证**：relfollow-verify.mjs 扩至 10 项全过（四边锚点 + 热区尺寸、线型/颜色设置与渲染跟随、快捷选项 + 自定义输入、字段改名值保留、预置字段为外貌+性格）；回归 chargraph 12/12、canvasctx 6/6、multiwindow 11/11、sidebar 5/5、canvas-model 25/25、olcanvas 29/29、dl 20/20、cdp-test 27/29（2 项存量）。

## v0.4.10（2026-09-04）关系图拖线改关系 + 新建人物空内容

- **拖线到已有关系 → 直接改变**：原先已有关系（含反向）时再拖线静默忽略；现在改为**打开该关系的编辑浮层**（沿用点边编辑浮层），原地改标签或删除——拖线成为"建立与改变关系"的统一手势；浮层定位在两节点连线中点（pickAnchors + edgeGeom）；
- **空标签默认「关联」**：拖线新建关系回车提交空标签时不再被丢弃，默认写入「关联」（与详情页添加行为一致）；
- **新建人物内容全空**：`addCharacter()` 默认名从「新人物」改为**空字符串**——姓名/别名/小传/字段全留空待填，角色默认配角；列表、画布节点、孤岛提示统一「未命名」兜底，角色徽标空时显示「未定」（侧栏「＋人物」与画布右键「＋ 新建人物」同走此默认）；
- **列表实时同步**：关系数据与画布同源（store.relations），图视图拖线建/改关系后，切到列表视图的「人物关系」区即时反映（本轮补齐无数据竞争）；侧栏列表同步显示新建的空人物；
- **验证**：新增 relfollow-verify.mjs 5/5（空标签默认关联、拖线到已有关系弹编辑浮层、改标签不重复建、详情区实时同步、右键新建空人物+未命名兜底）；回归 chargraph 12/12、canvasctx 6/6、multiwindow 11/11、dl 20/20、canvas-model 25/25、sidebar 5/5、olcanvas 29/29、cdp-test 27/29（2 项存量）。

## v0.4.9（2026-09-03）画布空白右键建模块 + Delete 删除

- **大纲画布：空白右键选建模块**——画布空白处右键弹出「在此处新建模块」菜单（◆ 事件 / 🗒 便签 / 🔗 引用 / ▭ 文本框 / ▢ 容器，章节锚点与卷因需选目标不入选单），新模块落在右键的画布坐标附近（复用 freeSpotFor 防重叠 + 吸附），创建即选中；便签自动带标题，引用卡保持既有目标选择流程；节点区域右键维持原行为（容器调色板），不弹创建菜单；
- **人物关系图：空白右键新建人物**——菜单「＋ 新建人物」，人物直接放到右键的画布坐标（布局即时落位 + scheduleSave），并自动选中；
- **Delete 键删除选中**：大纲画布 Delete/Backspace 删除当前选中模块（含其子树与指向连线，软删可撤销、可在回收站恢复）；人物关系图 Delete 删除选中人物（软删 + 关系与布局清理，可在回收站恢复）；输入框/行内编辑/连线编辑/拖线等输入态一律不劫持 Delete；
- **验证**：新增 canvasctx-verify.mjs 6/6（菜单出现与五类条目、选建落点、Delete 删模块、输入态守卫、人物右键建位、Delete 软删人物）；回归 canvas-model 25/25、sidebar 5/5、olcanvas 29/29、chargraph 12/12、multiwindow 11/11、dl 20/20、cdp-test 27/29（2 项存量）。

## v0.4.8（2026-09-03）面板完整界面化 + 实体锁跟随

- **面板升级为完整功能界面**：拆出的面板窗口不再是"单个条目窗口化"，而是与主窗口一致的完整模块界面——侧栏（章节树/大纲侧栏/人物列表/幕布树/灵感列表）+ 正文区（正文含右栏资源面板）+ 右缘调宽手柄，可在面板内自由切换章节/人物/条目；正文面板挂载双链悬浮预览与版本历史弹窗；拆窗入口文案改为模块级（⧉ 正文/大纲/人物/设定/灵感）；
- **实体锁随面板选中迁移**：新增 `panel:set-entity` IPC——章节/人物/灵感面板切换条目时，主进程锁表原子迁移（旧锁释放、新锁登记、`moved` 广播）；主窗口只读占位实时跟随（面板切到章节二 → 主窗口章节二只读、章节一恢复可写），「前往窗口」聚焦不受影响；
- **修复：面板自身被自己的锁误判为"他窗锁定"**——锁状态跟踪引入 ownKey（本面板当前持有键），isPopped 对自身持有键返回 false；修复后面板内编辑器保持可写，且另一面板占用同一条目时占位正确出现；
- **验证**：multiwindow-verify.mjs 扩至 11 项全过（新增：面板完整性断言——侧栏+编辑器+右栏齐备且自身可写；锁跟随断言——面板切章后主窗口两章占位状态正确迁移）；回归 canvas-model 25/25、sidebar 5/5、olcanvas 29/29、chargraph 12/12、dl 20/20、cdp-test 27/29（2 项存量）。

## v0.4.7（2026-09-03）侧栏调宽 + 画布文本按钮修复

- **侧栏拖拽调宽**：正文/大纲/人物/设定/灵感五大页签的侧栏右缘新增拖拽手柄（`.side-resizer`，悬停高亮），宽度范围 180–480px、越界自动钳制，松手写入 appconfig 记忆（重启保持），**双击手柄恢复默认 252px**；
- **修复：画布「整理」「适应」按钮文字溢出点击框**——`.om-btn` 原为 24×24px 单字符图标按钮（↶↷－＋设计），两个汉字放不下而溢出/换行；新增 `.om-btn.text` 汉字标签变体（自动宽度 + 内边距居中、不换行），应用于大纲画布「整理」「适应」与人物关系图「适应」；
- **验证**：新增 sidebar-verify.mjs 5/5（两画布按钮文字居框内、拖拽 ±位移生效、钳制 180–480、appconfig 记忆、双击复位）；回归 canvas-model 25/25、olcanvas 29/29、chargraph 12/12、dl 20/20、multiwindow 9/9、cdp-test 27/29（2 项存量）。

## v0.4.6（2026-09-03）功能面板多窗口（9.2-W5 首批）

- **模块拆出为独立面板窗口**：顶栏新增「⧉ 拆窗 ▾」，可把**正文（当前章节）/ 大纲（画布）/ 人物 / 设定（幕布）/ 灵感**五类模块拆出为独立窗口，与主窗口并行观看与操作（如主窗口写正文、副窗口看大纲画布）；面板窗口加载同一应用，按启动参数 `?panel=<type>:<workId>:<entityId>` 渲染精简布局（PanelHost：迷你工具条 = 实体名 + 保存状态 + 保存并关闭），既有功能组件零改动复用；
- **实体/模块级锁（两级锁的实体层）**：主进程锁表保证同作品同实体全应用仅一个可写面板窗口——重复拆出同一实体时自动**聚焦已开面板**而非二次创建；面板关闭自动释放锁并广播；
- **主窗口只读占位**：章节面板打开期间，主窗口该章节编辑器进入只读态（提示条「已在独立窗口编辑」+「前往窗口」按钮，标题/状态控件一并禁写，关闭面板自动恢复）；大纲/设定面板为模块级锁，主窗口对应页签整体替换为占位说明（侧栏与正文区同掩），人物/灵感按选中实体锁；
- **数据通道**：面板窗口持有自己的 work store 与 Dexie 连接（IndexedDB 多连接并发读，写经锁互斥），编辑保存复用现有 autosave 链路，关闭前走 `__flushNow` 优雅保存（与主窗口同策略）；
- **窗口生命周期与几何**：面板按类型记忆窗口几何（userData/panel-geometry.json，恢复时夹取到可见屏内）；主窗口关闭级联关闭全部面板；面板 `panel:event` 广播（opened/closed + 锁列表）供各窗口同步锁状态；
- **验证**：multiwindow-verify.mjs 9/9（拆窗创建 / 重复打开聚焦 / 只读占位出现与恢复 / 面板写入落库 / 关闭释放锁 / 大纲画布渲染 / 模块占位恢复）；回归 canvas-model 25/25、olcanvas 29/29、chargraph 12/12、dl 20/20、cdp-test 27/29（2 项存量）。

## v0.4.5（2026-09-03）连线贝塞尔方向修复

- **修复：容器向左连线的初段内折进容器内部**——贝塞尔控制点方向原先无条件向右（`c1 = a.x + dx`），向左的边从容器左缘锚点出发后初段先反向弯入容器（淡色内底可见该弯折，普通不透明节点被卡片遮住看不出）；改为控制点方向跟随连线走向（`dirx = b.x >= a.x ? 1 : -1`），向左的边初段直接向左出界；
- 同一修复作用于人物关系图（共用 edgeGeom）；
- **验证**：容器→左侧目标的贝塞尔 c1 在起点左侧断言通过、向右对照边方向正确；回归 olcanvas 29/29、chargraph 12/12、dl 20/20、canvas-model 25/25。

## v0.4.4（2026-09-03）连线层负坐标与对齐修复

- **修复：连线层整体错位**——v0.4.3 的负坐标视口修复把 SVG 元素平移到了视口原点，但漏加 `viewBox`，SVG 用户坐标系未随元素平移，path 的绝对画布坐标整体偏移 (svgBox.x, svgBox.y)，所有连线与节点错开；
- 补 `:viewBox="x y w h"`（与元素 style left/top 一致），画布坐标 → SVG 用户坐标 1:1 映射重新成立；
- **验证**：大纲画布（正/负坐标）与人物关系图（负坐标态）连线端点对齐节点边缘中点断言通过；viewBox 原点与元素位置一致性断言通过；回归 olcanvas 29/29、chargraph 12/12、canvas-model 25/25。

## v0.4.3（2026-09-03）关系图工具条改进 + 负坐标视口

- **人物关系图：移除节点常驻小工具条**——旧设计在选中节点右上角常驻「→纲 / 详情」两个按钮，遮挡头像与姓名且无法取消；改为**节点右键菜单**（发送为大纲引用卡 / 打开详情），随点随开、外点或滚轮关闭；双击节点打开详情的路径保留；
- **修复：模块向左/向上移动时连线消失**（大纲画布与人物关系图同样存在）——连线 SVG 视口从 (0,0) 起算，负坐标被裁剪，且包围盒向左/上收缩时视口跟着缩小误裁其余连线；改为视口覆盖负象限（svgBox 计算属性）并四周留 400px 余量，SVG 元素平移至视口原点保持画布坐标 1:1 映射；
- 清理失效测试用文件：`_map-verify.mjs`（OutlineMap 退役后失效）、`_shot.mjs`（无引用）、`_canvas-final.png`（截图产物改为 gitignore 不入库）。

## v0.4.2（2026-09-02）画布交互打磨与修复批次

- **画布交互打磨**：连线对齐接口（fromSide 随边存储 + 换侧自动回退）、实线/虚线/点线三线型（伏笔默认虚线）、连线粗细设置、标签中段上方显示、模块边缘调整大小、双击模块行内编辑（Ctrl+Enter 提交 / Esc 取消）、尺寸/位置变化 0.25s 缩放过渡动画；
- **容器增强**：与普通模块同级化（正文区行内编辑）、八向边缘 + 四角手柄调整尺寸（自定义尺寸为派生盒下限）、右键七色调色板 + 自动配色重置；
- **文本框独立模块**：原容器内嵌 PPT 式文本框升级为独立模块（虚线框、透明度滑杆 0.15–1、双链卫星、拖入容器即归组），旧内嵌文本惰性迁移；
- **侧栏层级树**：模块条目列表升级为层级树（容器从属缩进 + 折叠箭头，与画布同步；搜索/筛选平铺匹配项带容器前缀）；
- **文本视图联动**：选中容器列出子模块内容卡片，子模块显示所属容器面包屑；
- **交互修复批次**：
  - 模块可拖拽出容器（拖拽判定排除被拖子树，否则容器盒随节点扩张永远拖不出；拖出回根级、拖入即归组）；
  - 双击行内编辑修复（编辑态显式高度 + 解除 clip-path，五类形状编辑区完整可见）；
  - 菱形/平行四边形改 SVG polygon 描边（弃用 clip-path：其裁掉边框且锚点悬空；形状顶点恰在连线锚点上，hover/选中高亮）；
  - 事件高度拖拽 + 四角同时调宽高（非容器显式高度渲染；resize 只落库被拖轴）；
- **验收**：olcanvas-verify 29/29、canvas-model-test 25/25、chargraph-verify 12/12、dl-verify 20/20、cdp-test 27/29（2 项编辑器颜色命令存量，顺延 v0.4.3）。

## v0.4.1（2026-09-02）自由模块画布 + 人物关系图

- **大纲重构为流程图式白板**（计划书 8.8.2）：事件/便签/引用卡/容器/章节锚点自由摆位（网格吸附、双密度、pin、整理），四向锚点拖线（因果/伏笔/冲突/关联 + 箭头 + 标签原位编辑），贝塞尔/正交折线，容器拖入归组与折叠，断线检测（烂尾情节/缺铺垫），三幕式/章级清单骨架模板，olTypes 自定义类型，撤销重做；
- **人物关系图**（计划书 8.8.3）：头像节点 + 关系边可视化，画布拖线建关系、点边改标签（与关系列表双向同步），圆形/网格排布，悬停高亮一度关系网，角色/标签筛选，孤岛提示，发送为大纲引用卡；
- 数据迁移 v2（固定组降级容器、章节点转锚点，outlines 旧表保留）；graphview.js 画布引擎 + canvas-model.js 纯逻辑（node 直测）；
- 双实现统一：OutlineMap 退役（保留其侧栏与 olTypes）；
- 打包冒烟通过（231MB，pack-smoke 3/3）。

## v0.4.0（2026-09-01）大纲模块重构

- 大纲节点树 olnodes（一次性迁移）+ 思维导图视图（双链卫星/故事线脉络/缩放平移/双击跳转）+ 侧边栏自定义增删改 + Word 式工具栏；
- 修复 repo 层响应式对象直写 IndexedDB 的 DataCloneError 存量缺陷；
- 注：早期 v0.4.0 插件化尝试已回退，完整存档 `backup/v0.4.0-plugins` 分支。

## v0.3.x（2026-08-31 ~ 09-01）

- **v0.3.7**：版本推进（大纲优化实验回退，存档 `backup/v0.3.7-outline-text`）；
- **v0.3.6**：修复灵感「转为设定」写入已废弃 lore 表的数据死角（改创建幕布节点并跳转高亮）；
- **v0.3.5**：双链悬浮窗交互优化；正式启动程序精简（语言包裁剪 + afterPack 清理 + maximum 压缩，交付物 279MB→231MB）；
- **v0.3.4**：双链搜索升级为全局关键词检索（标题或正文命中 + 高亮选段 + 自动预填）；
- **v0.3.3**：多模块内容互联/双链上线（选中文字右键添加双链，跨模块链接与悬浮预览）；
- **v0.3.2**：书签系统（章节侧栏右键快捷栏 + 正文工具栏 ☆ 键 + 顶栏 🔖 下拉跳转）；
- **v0.3.1**：设定模块侧边栏（节点层级树/定位/双向高亮）、右键菜单对齐正文、`npm run stop` 完全退出机制、修复幕布编辑器 5 处缺陷（含合并丢子节点的数据丢失）；
- **v0.3.0**：设定库重构为幕布式自由大纲（取消文件夹/条目二分，统一节点树：拖拽三区指示/悬停展开/FLIP 动画/快照撤销/MD 导入/旧数据自动迁移）；取消安装程序与便携版（node_modules 精准裁剪 504MB→279MB）。

## v0.2.1（2026-08-30）

- 设定文件夹树 + 幕布编辑器工具栏/右键菜单 + MD 导入 + 总览视图 + 数据库占用明确提示。

## v0.1.x（2026-08 下旬）初版

- 核心写作一期：卷-章正文（TipTap 富文本）、大纲/人物/设定/灵感五模块、输入即存自动保存 + 版本快照、TXT/MD/DOCX/PDF/EPUB/图片导入导出、全局搜索、回收站、统计看板、三套主题。
