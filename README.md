# 小说工坊 Novel Studio

本地优先的小说创作软件（v0.3.0）。正文、大纲、人物、设定、灵感一站式管理，**输入即存**，永不丢稿。

> 分支说明：`main` 为基线；`v0.2.1-overview` 为功能开发分支（设定幕布化前的最后归档）；`main` 最新提交为 **v0.3.0 幕布式设定库重构**。

对应开发计划见 `../小说创作软件开发计划.md`（v1.1），本初版覆盖计划中 M0–M6 的核心功能。

## 快速开始

```bash
npm install        # 首次安装（约 3 分钟，Electron 较大）
npm run dev        # 开发模式（Vite 热更新 + Electron）
npm run build      # 构建渲染进程到 dist/
npm start          # 以生产模式启动应用（读取 dist/）
npm run web        # 网页版：启动本地静态服务器并打开浏览器（http://127.0.0.1:18688）
npm run dist       # 打包软件本体到 软件/win-unpacked/（双击 小说工坊.exe 直接运行，无安装程序）
```

要求：Node.js ≥ 18（开发环境为 Node 24）。`.npmrc` 已配置 Electron 国内镜像。

### 交付产物

| 形态 | 路径 / 用法 | 说明 |
|---|---|---|
| 软件本体 |  `软件/win-unpacked/小说工坊.exe` | 双击直接运行（免安装、免解压），`npm run dist` 重新生成 |
| 网页版 | 双击 `启动网页版.bat`，或 `npm run web` | 浏览器打开 http://127.0.0.1:18688，仅本机监听 |
| 源码运行 | `npm install && npm start` | 开发者方式 |

> v0.3.0 起取消安装程序与便携版打包（`release/`、`release2/` 中的旧安装包可全部删除），只保留可直接打开运行的软件本体；打包体积经 node_modules 精准裁剪由 504MB 降至 279MB。

注意事项：
- **同一时间只能运行一个实例**：安装版 / 便携版 / 开发模式共用数据目录（`%APPDATA%\小说工坊`），后启动的会显示「数据库被占用」提示（数据不会损坏）；需要多开时用 `小说工坊.exe --profile=名字` 走独立数据目录；
- 安装版 / 便携版 / 网页版的数据关系：桌面形态共用本地数据；网页版数据在浏览器 IndexedDB，与桌面版互相独立，跨形态迁移请用「备份 / 恢复」；
- 安装程序与 exe 未做代码签名，首次运行如遇 SmartScreen 提示，选「仍要运行」即可；
- 网页版中 Word 导入（.docx）依赖桌面端解析，暂不可用；PDF / EPUB / TXT / 图片导入不受影响。

## 功能总览

| 模块 | 功能 |
|---|---|
| 书架 | 封面卡片陈列作品，新建 / 编辑 / 删除（软删除），全库备份 / 恢复 |
| 正文 | 卷-章两级树，拖拽与按钮排序，TipTap 富文本编辑器 + Word 风格工具栏（加粗/斜体/下划线/删除线、正文/H1-H3、字号、字体颜色、突出显示、超链接、左中右两端对齐、列表、引用、撤销重做、清除格式），章节状态（草稿/完成/待修改），字数统计 |
| 大纲 | 总纲 → 卷纲 → 章纲三级，章纲与章节双联动（编辑章时右侧速览） |
| 人物 | 人物卡（定位/别名/标签/自定义字段/头像/小传），人物关系，本章出场人物自动检测 |
| 设定 | **幕布式自由大纲（v0.3.0 重构）**：整个设定库是一棵统一节点树，取消文件夹/条目二分，任意节点平权、可无限嵌套、自由拖拽（三区插入指示、悬停折叠节点自动展开、FLIP 衔接动画、拖到空白处移到顶层）；节点支持内联格式（工具栏与右键菜单：加粗/斜体/下划线/删除线/颜色/底色/清除格式 + 快照式撤销重做 + 节点复制/剪切/粘贴），右键菜单与正文编辑器同构（文字剪切/复制/粘贴为纯文本优先，选区内右键保持选区）；**侧边栏**：节点层级树实时同步，点击定位滚动高亮；**MD/TXT 导入**：标题层级自动转为节点层级，正文行转为子节点；旧「分类/条目」数据首次打开自动无损迁移；回收站支持节点恢复 |
| 灵感 | 随手记，一键转为章节 / 设定 |
| 自动保存 | 输入防抖 1s 落盘 + 30s 兜底 + 失焦/切章/关闭前强制保存；顶部常驻保存状态指示 |
| 版本快照 | 章节内容变更按策略（5 分钟冷却）自动快照，保留最近 30 份，可一键回滚 |
| 回收站 | 正文/人物/设定/灵感软删除，可恢复或彻底删除 |
| 导入 | TXT / Markdown / DOCX / PDF / EPUB / 常见图片；TXT 自动识别 GBK 等编码、按"第X章"智能分章；DOCX 按标题样式分章；PDF 按书签分章（扫描件可转存图片）；导入前预览 |
| 导出 | 整库备份 JSON（含图片）、本书 TXT / Markdown |
| 链接安全 | 超链接仅接受公网 http/https 地址（拒绝本地/私有/保留 IP），点击时在系统默认浏览器打开，不劫持应用窗口 |
| 右键菜单 | 正文编辑区右键打开 Word 式快捷操作栏：剪切/复制/粘贴为纯文本（剪贴板走安全 IPC）、格式化、标题/字号/字体颜色/突出显示/对齐二级飞出菜单、列表引用、超链接（含在链接上右键时的编辑/打开/清除）、撤销重做/全选/清除格式；右键落在选区内保持选区，落在选区外先将光标移至该处 |
| 搜索 | 跨正文/大纲/人物/设定/灵感全局搜索，命中高亮、点击跳转 |
| 统计 | 每日新增字数柱状图、180 天写作热力图、总字数/周新增/写作天数卡片（ECharts） |
| 主题 | 宣纸 / 羊皮纸 / 暗夜书房三套皮肤，衬线排版，纸张纹理，沉浸模式（Ctrl+Shift+F 全局搜索） |

## 数据与安全

- 所有数据存储在本机 IndexedDB（Electron 用户数据目录），**无任何网络请求**；
- 菜单「备份」导出 JSON 全库快照（含图片 base64），「恢复」可整体还原；
- 数据库按 SQLite 单文件思路设计（Repository 层隔离），后续可无缝迁移到 SQLite。

## 与开发计划的差异（初版取舍）

| 项 | 现状 |
|---|---|
| 存储引擎 | 计划默认 SQLite，初版采用 IndexedDB（同为本地持久化，免原生模块编译），Repository 层（`src/db/repo.js`）已隔离，迁移 SQLite 只改这一层 |
| 编辑器 | 一期为 Markdown 纯文本，应用户需求已升级为 TipTap 富文本（正文以 HTML 存储，章节带 `fmt` 标记）；旧 Markdown/纯文本章节在打开编辑时自动转换为 HTML，首次保存前自动生成原格式快照可回滚；TXT/MD 导出时反向转换 |
| 打字机模式 | 未实现（沉浸模式已实现），列入下版 |
| OCR / 老版 .doc / mobi | 未支持，按计划列二期 |
| 安装包 | electron-builder 打包配置未接入，当前以 `npm start` 运行，打包列入下版 |
| 快照冷却 | 新章节首次快照需编辑满 5 分钟后才生成，属预期防抖设计 |

## 测试

`tools/cdp-test.mjs` 为端到端验收脚本（18 项），通过 Electron 远程调试端口驱动，不抢占前台：

```bash
npx electron . --remote-debugging-port=9222   # 终端 1
node tools/cdp-test.mjs                        # 终端 2：跑验收
node tools/post-verif.mjs verify               # 崩溃恢复验收（先 taskkill 强杀再启动后执行）
```

最近一次验收：全量功能 **18/18**、右键菜单 **15/15**、幕布式设定大纲 **8/8**（真实键盘事件）通过，崩溃恢复（强杀进程）通过；演示作品《星尘旅人》由 `tools/post-verif.mjs seed` 生成，可删除后自建。

## 目录结构

```
electron/        主进程（窗口、app:// 静态协议、导入导出 IPC）
src/
  db/            表结构定义 + Repository 层（存储引擎切换点）
  services/      自动保存、导入解析、导出、文件 IO
  stores/        Pinia 状态（书架 / 作品 / 界面）
  components/    书架、工作台、编辑器、各要素面板与弹窗
  styles/        三套主题皮肤
tools/           E2E 验收脚本与截图
```
